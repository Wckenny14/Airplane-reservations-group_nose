<%= form_with(model: flights_seat, local: true) do |form| %>
  <% if flights_seat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(flights_seat.errors.count, "error") %> prohibited this flights_seat from being saved:</h2>

      <ul>
      <% flights_seat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :seat_id %>
    <%= form.number_field :seat_id %>
  </div>

  <div class="field">
    <%= form.label :flight_id %>
    <%= form.number_field :flight_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
